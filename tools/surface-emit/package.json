{
  "name": "haven-doc-emit-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spike: prove the content<->component seam — portable markdown + nav manifest -> build-time AST rewrite -> static HTML styled by haven-ui semantic classes. Isolated from the pnpm workspace (spikes/* is not a workspace glob).",
  "scripts": {
    "emit": "node emit.mjs"
  },
  "dependencies": {
    "elkjs": "^0.11.1",
    "gray-matter": "^4.0.3",
    "hastscript": "^9.0.0",
    "rehype-stringify": "^10.0.1",
    "remark-directive": "^3.0.0",
    "remark-gfm": "^4.0.0",
    "remark-parse": "^11.0.0",
    "remark-rehype": "^11.1.1",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.0.0"
  }
}
