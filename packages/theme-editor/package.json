{
  "name": "@haven/theme-editor",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based theme editor — authors portable preset JSON and emits theme.css for haven-obsidian-theme (v1 target). Pattern travels beyond Obsidian.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "culori": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
