# Retro Log

- 2026-05-25 — **Pipeline framework-emit analysis (AD-08 revisit).** Aaron asked whether the pipeline should emit Angular (functional app updates) instead of HTML Andrey reimplements; recovered that AD-08 (React, 2026-04-18) already decided this but on a premise ("we own the full frontend") that lapsed, and that the *mechanical-emit-from-locked-spec* angle was never evaluated. Produced ADR `planning/team/proposals/AD-08-revisit-pipeline-framework-emit.md` ranking three delivery paths under the ownership bar. **Verdict:** hold Path A (HTML handoff — clears the ownership bar today) as the floor, run a thin Path C (Angular mechanical emit) proving slice (hand-port quantity-stepper), promote C if it ports mechanically AND Andrey accepts generated Angular; deprioritize Path B (React ownership — premise lapsed, unproven, max Andrey demand). Key honest finding: "functional, not HTML" is overstated by exactly the unbuilt T0.2 data-layer gap (0% data-binding for all paths until Andrey's service contract exists). Dispatched unprimed (3 paths framed as equals) per avoid-priming. Outcome: decision-useful proposal for Aaron; T0.1/T0.2 questions routed to Andrey + Vanessa. Confidence: med-high on ranking logic, medium on C promotion (decisive variable — does generated Angular reduce or just shift Andrey's work — only Andrey can answer).
